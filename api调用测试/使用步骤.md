# api调用测试 — DashScope LLM（项目唯一入口）

## 配置

```powershell
cd "C:\Users\DELL\Desktop\icu\BME_1325_Group4_repo\api调用测试"
copy .env.example .env
# 编辑 .env 填入 DASHSCOPE_API_KEY
```

## 验证

```powershell
pip install -r requirements.txt
python api使用.py
```

## 后端 Agent

- `system/backend/api/.env`：`ICU_LLM_ENABLED=true`
- 密钥/模型自动从本目录 `.env` 加载
- 修改后需重启 uvicorn

已弃用教学网 GenAI（`genaiapi.shanghaitech.edu.cn`）。
